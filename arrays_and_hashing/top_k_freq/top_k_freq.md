I first create a dict to keep track of which items occurred how much by looping through the input list. I then use the sorted method to sort the items by their count (or values) and set them in a new dictionary. Using enumerate, I loop over the dictionary and get the key and values as well the iteration count. As long as the count is under the specified number, we append top most frequent numbers to a list.
