My solution use a nested loop solution to check if a word from one string exits in the other string and removes it from both if so. By the end of the outer loop, if both strings are empty, they are a match regardless of the word order. However, a faster solution would be to use the sorted method where sorting in that way will allow the string lists to be checked word for word without any nested looping.
